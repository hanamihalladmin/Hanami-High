"use client";

import {useCallback,useEffect,useState} from "react";

const SUPABASE_URL=process.env.NEXT_PUBLIC_SUPABASE_URL??"https://mperfphbhqpjlqmaysmg.supabase.co";
const SUPABASE_PUBLISHABLE_KEY=process.env.NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY??"sb_publishable_G-Pg-XwLz6rpRdlIWXcIgg_kxyd4gb0";
function headers(token:string,extra:Record<string,string>={}){return {apikey:SUPABASE_PUBLISHABLE_KEY,Authorization:`Bearer ${token}`,...extra};}
type Bulletin={bell?:{label:string;period_kind:string;starts_at:string|null;ends_at:string|null;is_override:boolean;detail:string};announcements?:Array<{id:string;title:string;body:string;severity:string;published_at:string}>;events?:Array<{id:string;title:string;starts_at:string;ends_at:string|null;location:string|null;category:string}>;club_meetings?:Array<{id:string;title:string;starts_at:string;location:string|null;activity_name:string}>;session?:{title:string;school_year:string;chapter_name:string;season:string;status:string}|null;competition?:Array<{title:string;code:string;points:number}>;exam_mode?:{enabled:boolean;title:string;starts_on:string|null;ends_on:string|null;announcement:string;temporary_style:string}|null;my_exams?:Array<{exam_title:string;exam_at:string;room:string|null;seat_number:string|null;review_materials:string}>;my_seats?:Array<{section_code:string;seat_label:string;row_number:number|null;column_number:number|null}>;substitutes?:Array<{section_code:string;substitute_name:string;starts_at:string;ends_at:string;note:string}>};
export default function DailySchoolBulletinPanel({accessToken,characterId}:{accessToken:string;characterId:string}){const [data,setData]=useState<Bulletin>({});const [notice,setNotice]=useState("Loading today's bulletin…");const load=useCallback(async()=>{const r=await fetch(`${SUPABASE_URL}/rest/v1/rpc/daily_school_bulletin`,{method:"POST",headers:headers(accessToken,{"Content-Type":"application/json"}),body:JSON.stringify({target_character_id:characterId})});if(!r.ok){setNotice("Daily bulletin could not be loaded.");return;}setData(await r.json());setNotice("Updated from Hanami school systems in Tokyo time.");},[accessToken,characterId]);useEffect(()=>{void load();const t=setInterval(()=>void load(),60000);return()=>clearInterval(t);},[load]);const fmt=(v:string)=>new Intl.DateTimeFormat("en-US",{timeZone:"Asia/Tokyo",hour:"numeric",minute:"2-digit"}).format(new Date(v));return <section style={{border:"1px solid #c7cdd4",background:"#fff",marginTop:0}}><div style={{padding:"14px 16px",background:data.exam_mode?.enabled?"#fff8e5":"#fff",borderBottom:"1px solid #c7cdd4",display:"flex",justifyContent:"space-between",gap:10,flexWrap:"wrap"}}><div><p className="eyebrow">DAILY SCHOOL BULLETIN • TOKYO</p><h4 style={{margin:"4px 0",font:"500 20px system-ui,sans-serif"}}>{data.bell?.label??"School Day Status"}</h4><small>{data.bell?.is_override?`SPECIAL SCHEDULE • ${data.bell.detail||"Temporary bell override"}`:notice}</small></div><div style={{textAlign:"right"}}>{data.exam_mode?.enabled&&<strong style={{display:"block"}}>EXAMS WEEK MODE</strong>}{data.session&&<span style={{fontSize:9}}>{data.session.school_year} • {data.session.chapter_name} • {data.session.season}</span>}</div></div><div style={{display:"grid",gridTemplateColumns:"repeat(auto-fit,minmax(220px,1fr))"}}><article style={{padding:14,borderRight:"1px solid #e1e5e9",borderBottom:"1px solid #e1e5e9"}}><strong>Announcements</strong>{data.announcements?.length?data.announcements.map(a=><div key={a.id} style={{marginTop:7,fontSize:9}}><b>{a.severity?.toUpperCase()} • {a.title}</b><div>{a.body.slice(0,140)}{a.body.length>140?"…":""}</div></div>):<p style={{fontSize:9}}>No current announcements.</p>}</article><article style={{padding:14,borderRight:"1px solid #e1e5e9",borderBottom:"1px solid #e1e5e9"}}><strong>Today&apos;s Events</strong>{data.events?.length?data.events.map(e=><div key={e.id} style={{marginTop:7,fontSize:9}}><b>{fmt(e.starts_at)} • {e.title}</b><div>{e.location??"Location TBA"}</div></div>):<p style={{fontSize:9}}>No school events today.</p>}</article><article style={{padding:14,borderBottom:"1px solid #e1e5e9"}}><strong>Club Meetings</strong>{data.club_meetings?.length?data.club_meetings.map(e=><div key={e.id} style={{marginTop:7,fontSize:9}}><b>{e.activity_name}</b> • {fmt(e.starts_at)}<div>{e.title}{e.location?` • ${e.location}`:""}</div></div>):<p style={{fontSize:9}}>No club meetings listed today.</p>}</article>{data.competition&&data.competition.length>0&&<article style={{padding:14}}><strong>Class Competition</strong>{data.competition.slice(0,5).map((r,i)=><div key={`${r.title}-${r.code}`} style={{fontSize:9,marginTop:5}}>{i+1}. <b>{r.code}</b> — {r.points} pts</div>)}</article>}{data.my_seats&&data.my_seats.length>0&&<article style={{padding:14}}><strong>My Classroom Seats</strong>{data.my_seats.map(s=><div key={`${s.section_code}-${s.seat_label}`} style={{fontSize:9,marginTop:5}}><b>{s.section_code}</b> • Seat {s.seat_label}</div>)}</article>}{data.substitutes&&data.substitutes.length>0&&<article style={{padding:14}}><strong>Substitute Teachers</strong>{data.substitutes.slice(0,5).map(s=><div key={`${s.section_code}-${s.starts_at}`} style={{fontSize:9,marginTop:5}}><b>{s.section_code}</b> • {s.substitute_name} • {fmt(s.starts_at)}</div>)}</article>}{data.exam_mode?.enabled&&<article style={{padding:14}}><strong>{data.exam_mode.title}</strong><p style={{fontSize:9}}>{data.exam_mode.announcement||"Temporary exam schedule is active."}</p>{data.my_exams?.map(ex=><div key={`${ex.exam_title}-${ex.exam_at}`} style={{fontSize:9,marginTop:6}}><b>{ex.exam_title}</b> • {fmt(ex.exam_at)} • {ex.room??"Room TBA"}{ex.seat_number?` • Seat ${ex.seat_number}`:""}{ex.review_materials&&<div>{ex.review_materials}</div>}</div>)}</article>}</div></section>;}
