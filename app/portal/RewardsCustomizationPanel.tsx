"use client";

import {useCallback,useEffect,useMemo,useState} from "react";
import styles from "./RewardsCustomizationPanel.module.css";

const U=process.env.NEXT_PUBLIC_SUPABASE_URL??"https://mperfphbhqpjlqmaysmg.supabase.co";
const K=process.env.NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY??"sb_publishable_G-Pg-XwLz6rpRdlIWXcIgg_kxyd4gb0";

type Wallet={user_id:string;balance:number;lifetime_earned:number};
type StoreItem={id:string;slug:string;label:string;category:string;description:string;cost:number;asset_url:string|null;rarity:string;cosmetic_slot:string|null;bloom_required:boolean;metadata:Record<string,unknown>};
type Ownership={item_id:string;acquired_at:string};
type Equipped={slot:string;item_id:string};
type Entitlement={entitlement_key:string};
type Transaction={id:string;amount:number;reason:string;created_at:string};
type AchievementDefinition={id:string;label:string;description:string;icon:string;token_reward:number};
type CharacterAchievement={achievement_id:string;awarded_at:string;note:string};
type Mode="rewards"|"exchange";

function h(token:string,extra:Record<string,string>={}){return{apikey:K,Authorization:`Bearer ${token}`,...extra}}

export default function RewardsCustomizationPanel({accessToken,characterId,mode}:{accessToken:string;characterId:string;mode:Mode}){
 const [wallet,setWallet]=useState<Wallet|null>(null);const [items,setItems]=useState<StoreItem[]>([]);const [owned,setOwned]=useState<Ownership[]>([]);const [equipped,setEquipped]=useState<Equipped[]>([]);const [entitlements,setEntitlements]=useState<Entitlement[]>([]);const [transactions,setTransactions]=useState<Transaction[]>([]);const [achievementDefs,setAchievementDefs]=useState<AchievementDefinition[]>([]);const [achievements,setAchievements]=useState<CharacterAchievement[]>([]);const [busy,setBusy]=useState("");const [message,setMessage]=useState("");
 const ownedIds=useMemo(()=>new Set(owned.map(row=>row.item_id)),[owned]);const bloom=entitlements.some(row=>row.entitlement_key==="hanami_bloom");
 const load=useCallback(async()=>{if(!characterId)return;setMessage("");try{
  await fetch(`${U}/rest/v1/rpc/ensure_current_petal_wallet`,{method:"POST",headers:h(accessToken,{"Content-Type":"application/json"}),body:"{}"});
  const [walletR,itemR,ownedR,equippedR,entitlementR,transactionR,definitionR,achievementR]=await Promise.all([
   fetch(`${U}/rest/v1/petal_wallets?select=user_id,balance,lifetime_earned&limit=1`,{headers:h(accessToken),cache:"no-store"}),
   fetch(`${U}/rest/v1/school_store_items?select=id,slug,label,category,description,cost,asset_url,rarity,cosmetic_slot,bloom_required,metadata&active=eq.true&order=cost.asc,label.asc`,{headers:h(accessToken),cache:"no-store"}),
   fetch(`${U}/rest/v1/account_store_ownership?select=item_id,acquired_at&order=acquired_at.desc`,{headers:h(accessToken),cache:"no-store"}),
   fetch(`${U}/rest/v1/character_equipped_store_items?select=slot,item_id&character_id=eq.${encodeURIComponent(characterId)}`,{headers:h(accessToken),cache:"no-store"}),
   fetch(`${U}/rest/v1/account_entitlements?select=entitlement_key`,{headers:h(accessToken),cache:"no-store"}),
   fetch(`${U}/rest/v1/petal_transactions?select=id,amount,reason,created_at&order=created_at.desc&limit=12`,{headers:h(accessToken),cache:"no-store"}),
   fetch(`${U}/rest/v1/achievement_definitions?select=id,label,description,icon,token_reward&active=eq.true`,{headers:h(accessToken),cache:"no-store"}),
   fetch(`${U}/rest/v1/character_achievements?select=achievement_id,awarded_at,note&character_id=eq.${encodeURIComponent(characterId)}&order=awarded_at.desc`,{headers:h(accessToken),cache:"no-store"})
  ]);
  if(walletR.ok)setWallet(((await walletR.json()) as Wallet[])[0]??null);if(itemR.ok)setItems(await itemR.json() as StoreItem[]);if(ownedR.ok)setOwned(await ownedR.json() as Ownership[]);if(equippedR.ok)setEquipped(await equippedR.json() as Equipped[]);if(entitlementR.ok)setEntitlements(await entitlementR.json() as Entitlement[]);if(transactionR.ok)setTransactions(await transactionR.json() as Transaction[]);if(definitionR.ok)setAchievementDefs(await definitionR.json() as AchievementDefinition[]);if(achievementR.ok)setAchievements(await achievementR.json() as CharacterAchievement[]);
 }catch{setMessage("Rewards data could not be loaded.")}},[accessToken,characterId]);
 useEffect(()=>{void load()},[load]);
 async function purchase(item:StoreItem){setBusy(item.id);setMessage("");try{const response=await fetch(`${U}/rest/v1/rpc/purchase_exchange_item`,{method:"POST",headers:h(accessToken,{"Content-Type":"application/json"}),body:JSON.stringify({target_item_id:item.id})});if(!response.ok){const body=await response.json().catch(()=>null) as {message?:string}|null;throw new Error(body?.message||"Purchase failed");}setMessage(`${item.label} added to your account.`);await load();}catch(error){setMessage(error instanceof Error?error.message:"Purchase failed");}finally{setBusy("")}}
 async function equip(item:StoreItem){if(!item.cosmetic_slot)return;setBusy(item.id);setMessage("");try{const response=await fetch(`${U}/rest/v1/character_equipped_store_items?on_conflict=character_id,slot`,{method:"POST",headers:h(accessToken,{"Content-Type":"application/json",Prefer:"resolution=merge-duplicates,return=minimal"}),body:JSON.stringify({character_id:characterId,slot:item.cosmetic_slot,item_id:item.id})});if(!response.ok)throw new Error("Could not equip this cosmetic");setMessage(`${item.label} equipped for this character.`);await load();window.dispatchEvent(new CustomEvent("hanami-portal-theme-changed",{detail:{characterId}}));}catch(error){setMessage(error instanceof Error?error.message:"Equip failed");}finally{setBusy("")}}
 async function unequip(slot:string){setBusy(slot);setMessage("");try{const response=await fetch(`${U}/rest/v1/character_equipped_store_items?character_id=eq.${encodeURIComponent(characterId)}&slot=eq.${encodeURIComponent(slot)}`,{method:"DELETE",headers:h(accessToken,{Prefer:"return=minimal"})});if(!response.ok)throw new Error("Could not unequip this cosmetic");await load();window.dispatchEvent(new CustomEvent("hanami-portal-theme-changed",{detail:{characterId}}));}catch(error){setMessage(error instanceof Error?error.message:"Unequip failed");}finally{setBusy("")}}
 const definitionsById=useMemo(()=>new Map(achievementDefs.map(row=>[row.id,row])),[achievementDefs]);
 return <section className={styles.wrap} aria-label={mode==="exchange"?"Hanami Exchange":"Rewards and achievements"}><header className={styles.hero}><div><p className={styles.eyebrow}>{mode==="exchange"?"Hanami Exchange":"Participation Rewards"}</p><h2>{mode==="exchange"?"Collect, own, and equip cosmetics":"Petals, achievements, and prestige"}</h2><p>{mode==="exchange"?"Petals are earned through participation and cannot be purchased. Exchange items belong to your account; equipped cosmetics belong to the active character.":"Your Petal wallet belongs to your account, while achievements stay with the character who earned them."}</p></div><div className={styles.wallet}><span>Petals</span><strong>{wallet?.balance??0}</strong><small>{wallet?.lifetime_earned??0} lifetime earned</small></div></header><div className={styles.badges}><span className={styles.badge}>Account-owned wallet</span><span className={styles.badge}>Character equip slots</span><span className={bloom?styles.bloom:styles.badge}>Hanami Bloom: {bloom?"Active":"Not active"}</span></div>{message&&<p className={styles.notice}>{message}</p>}{mode==="exchange"?<><div className={styles.sectionHead}><div><h3>Exchange catalog</h3><p>Only approved cosmetics can alter appearance; portal geometry stays locked.</p></div></div>{items.length===0?<div className={styles.empty}><strong>The Exchange catalog is empty.</strong><span>Owner/Admin catalog publishing will populate this space without changing the portal shell.</span></div>:<div className={styles.grid}>{items.map(item=>{const isOwned=ownedIds.has(item.id);const isEquipped=equipped.some(row=>row.item_id===item.id);const locked=item.bloom_required&&!bloom;return <article className={styles.card} key={item.id}><div className={styles.cardTop}><span>{item.category}</span><em>{item.rarity}</em></div><h4>{item.label}</h4><p>{item.description}</p><div className={styles.cardFooter}><strong>{item.cost} Petals</strong>{locked?<button disabled>Bloom required</button>:!isOwned?<button disabled={busy===item.id||(wallet?.balance??0)<item.cost} onClick={()=>void purchase(item)}>{busy===item.id?"Working…":"Collect"}</button>:item.cosmetic_slot?(isEquipped?<button disabled={busy===item.cosmetic_slot} onClick={()=>void unequip(item.cosmetic_slot!)}>Unequip</button>:<button disabled={busy===item.id} onClick={()=>void equip(item)}>Equip</button>):<span className={styles.owned}>Owned</span>}</div></article>})}</div>}</>:<div className={styles.columns}><section className={styles.panel}><h3>Recent Petal activity</h3>{transactions.length===0?<p className={styles.muted}>No Petal activity yet.</p>:transactions.map(row=><div className={styles.row} key={row.id}><span><strong>{row.reason}</strong><small>{new Date(row.created_at).toLocaleDateString()}</small></span><b className={row.amount>=0?styles.positive:styles.negative}>{row.amount>=0?`+${row.amount}`:row.amount}</b></div>)}</section><section className={styles.panel}><h3>Character achievements</h3>{achievements.length===0?<p className={styles.muted}>No achievements earned yet.</p>:achievements.map(row=>{const def=definitionsById.get(row.achievement_id);return <div className={styles.row} key={`${row.achievement_id}-${row.awarded_at}`}><span><strong>{def?.icon?`${def.icon} `:""}{def?.label??"Achievement"}</strong><small>{def?.description??row.note}</small></span>{def&&def.token_reward>0?<b>+{def.token_reward}</b>:null}</div>})}</section></div>}</section>;
}
