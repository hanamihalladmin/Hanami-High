import { useCallback, useEffect, useMemo, useState } from 'react'
import { supabase } from '../lib/supabase'
import { useIdentity } from '../state/IdentityContext'
import type { BoutiqueItem, HanamiPlusEntitlement, InventoryItem, PetalWallet } from '../types/database-rewards'
import type { BoutiqueWishlistRow } from '../types/database-customization'
import { BoutiqueArtwork } from './BoutiqueArtwork'
import { ShellTopbar } from './ShellTopbar'

type Mode='featured'|'new'|'seasonal'|'avatar-decorations'|'frames'|'effects'|'nameplates'|'profile-cards'|'background-packs'|'stickers'|'hanami-plus-passes'|'my-inventory'
type Props={mode:Mode;onSearch:()=>void;onNotifications:()=>void;unreadCount:number}
const modeMeta:Record<Mode,{title:string;description:string}>={featured:{title:'Featured',description:'Hanami’s current collectible drops and highlighted cosmetics.'},new:{title:'New',description:'Recently added cosmetics, decorations, cards, and effects.'},seasonal:{title:'Seasonal',description:'Limited collections tied to the 2006 Hanami school year.'},'avatar-decorations':{title:'Avatar Decorations',description:'Collectible decorations that frame and orbit your avatar.'},frames:{title:'Frames',description:'Profile and avatar frames for your Hanami identity card.'},effects:{title:'Effects',description:'Decorative profile effects and animated-style visual treatments.'},nameplates:{title:'Nameplates',description:'Character nameplate styles for profiles and social surfaces.'},'profile-cards':{title:'Profile Cards',description:'Alternative identity-card treatments for your profile.'},'background-packs':{title:'Background Packs',description:'Collectible background scenes for profile customization.'},stickers:{title:'Stickers',description:'Sticker sheets and decorative profile pieces.'},'hanami-plus-passes':{title:'Hanami+ Passes',description:'Time-limited Hanami+ cosmetic access purchased with Petals.'},'my-inventory':{title:'My Inventory',description:'Everything currently owned by this account.'}}
const typeLabels:Record<string,string>={avatar_decoration:'Avatar Decoration',frame:'Frame',effect:'Profile Effect',nameplate:'Nameplate',profile_card:'Profile Card',background_pack:'Background Pack',sticker:'Sticker',hanami_plus_pass:'Hanami+ Pass'}

export function BoutiquePage({mode,onSearch,onNotifications,unreadCount}:Props){
 const {account}=useIdentity();const[wallet,setWallet]=useState<PetalWallet|null>(null);const[items,setItems]=useState<BoutiqueItem[]>([]);const[inventory,setInventory]=useState<InventoryItem[]>([]);const[wishlist,setWishlist]=useState<BoutiqueWishlistRow[]>([]);const[plus,setPlus]=useState<HanamiPlusEntitlement|null>(null);const[loading,setLoading]=useState(true);const[working,setWorking]=useState<string|null>(null);const[error,setError]=useState<string|null>(null);const[notice,setNotice]=useState<string|null>(null);const[previewId,setPreviewId]=useState<string|null>(null)
 const load=useCallback(async()=>{const client=supabase;if(!client||!account)return;setLoading(true);setError(null);const[w,i,n,h,l]=await Promise.all([client.from('petal_wallets').select('*').eq('account_id',account.id).maybeSingle(),client.from('boutique_items').select('*').eq('state','published').order('featured',{ascending:false}).order('created_at',{ascending:false}),client.from('inventory_items').select('*').eq('account_id',account.id).order('acquired_at',{ascending:false}),client.from('hanami_plus_entitlements').select('*').eq('account_id',account.id).maybeSingle(),client.from('boutique_wishlist').select('*').eq('account_id',account.id).order('created_at',{ascending:false})]);const first=[w.error,i.error,n.error,h.error,l.error].find(Boolean);setLoading(false);if(first)return setError(first.message);setWallet(w.data);setItems(i.data??[]);setInventory(n.data??[]);setPlus(h.data);setWishlist(l.data??[])},[account]);useEffect(()=>{void load()},[load])
 const owned=useMemo(()=>new Map(inventory.map(i=>[i.item_id,i])),[inventory]);const wished=useMemo(()=>new Set(wishlist.map(i=>i.item_id)),[wishlist]);
 const visible=useMemo(()=>{if(mode==='my-inventory')return items.filter(i=>owned.has(i.id));if(mode==='featured')return items.filter(i=>i.featured);if(mode==='new')return items.filter(i=>i.is_new);if(mode==='seasonal')return items.filter(i=>i.season!=='permanent');const map:Record<string,string>={'avatar-decorations':'avatar_decoration',frames:'frame',effects:'effect',nameplates:'nameplate','profile-cards':'profile_card','background-packs':'background_pack',stickers:'sticker','hanami-plus-passes':'hanami_plus_pass'};return items.filter(i=>i.item_type===map[mode])},[items,mode,owned])
 const hero=useMemo(()=>items.find(i=>i.id===previewId)||visible.find(i=>i.featured)||visible[0]||items.find(i=>i.featured)||items[0]||null,[items,previewId,visible]);const plusActive=plus&&new Date(plus.ends_at).getTime()>Date.now()
 async function purchase(item:BoutiqueItem){const client=supabase;if(!client)return;setWorking(item.id);setError(null);setNotice(null);const{data,error:e}=await client.rpc('purchase_boutique_item',{p_item_id:item.id,p_request_id:crypto.randomUUID()});setWorking(null);if(e)return setError(e.message);setNotice(`${item.name} added to your account. ${data?.[0]?.balance??wallet?.balance??0} Petals remaining.`);await load()}
 async function toggleWish(item:BoutiqueItem){const client=supabase;if(!client||!account)return;setError(null);if(wished.has(item.id)){const{error:e}=await client.from('boutique_wishlist').delete().eq('account_id',account.id).eq('item_id',item.id);if(e)return setError(e.message);setWishlist(c=>c.filter(r=>r.item_id!==item.id));setNotice(`${item.name} removed from your Wishlist.`)}else{const{data,error:e}=await client.from('boutique_wishlist').insert({account_id:account.id,item_id:item.id}).select('*').single();if(e)return setError(e.message);setWishlist(c=>[data,...c]);setNotice(`${item.name} added to your Wishlist.`)}}
 function buyButton(item:BoutiqueItem,compact=false){const row=owned.get(item.id),enough=(wallet?.balance??0)>=item.price_petals,repeat=item.item_type==='hanami_plus_pass',can=enough&&(!row||repeat);if(mode==='my-inventory')return <span className="boutique-owned-badge">Owned ×{row?.quantity??1}</span>;return <button className="boutique-buy-button" disabled={!can||working===item.id} onClick={e=>{e.stopPropagation();void purchase(item)}}>{working===item.id?'Purchasing…':row&&!repeat?'Owned':!enough?'Need more Petals':compact?`❀ ${item.price_petals}`:`Purchase · ❀ ${item.price_petals}`}</button>}
 function heart(item:BoutiqueItem){return <button className={`boutique-heart ${wished.has(item.id)?'wished':''}`} type="button" title={wished.has(item.id)?'Remove from Wishlist':'Add to Wishlist'} onClick={e=>{e.stopPropagation();void toggleWish(item)}}>{wished.has(item.id)?'♥':'♡'}</button>}
 return <main className="content-area boutique-page boutique-storefront"><ShellTopbar eyebrow="BOUTIQUE" title={modeMeta[mode].title} onSearch={onSearch} onNotifications={onNotifications} unreadCount={unreadCount}/><div className="boutique-orbs-nav"><strong>✿ HANAMI BOUTIQUE</strong><nav><a href="#/boutique/featured">Featured</a><a href="#/boutique/new">Browse</a><a href="#/boutique/avatar-decorations">Decorations</a><a href="#/boutique/my-inventory">My Inventory</a></nav><a className="boutique-wishlist-link" href="#/profile/profile-studio">♥ {wishlist.length}</a><span>❀ {wallet?.balance??0}</span></div><div className="boutique-storebar"><div><span className="eyebrow">HANAMI COLLECTIBLES</span><p>{modeMeta[mode].description}</p></div><div className="boutique-wallet-chip"><span>YOUR PETALS</span><strong>❀ {wallet?.balance??0}</strong>{plusActive&&<small>Hanami+ · {new Date(plus!.ends_at).toLocaleDateString()}</small>}</div></div>{error&&<div className="identity-notice error">{error}</div>}{notice&&<div className="identity-notice success">{notice}</div>}{loading?<div className="rewards-empty">Loading the Boutique…</div>:<>{hero&&mode!=='my-inventory'&&<section className={`boutique-feature-hero rarity-${hero.rarity}`}><div className="boutique-feature-art"><BoutiqueArtwork token={hero.preview_token} name={hero.name} type={hero.item_type} large/>{heart(hero)}</div><div className="boutique-feature-copy"><div className="boutique-feature-tags"><span>{typeLabels[hero.item_type]||hero.item_type}</span><span>{hero.rarity}</span>{hero.is_new&&<span>NEW</span>}</div><span className="eyebrow">{hero.collection_name||`${hero.season} collection`}</span><h1>{hero.name}</h1><p>{hero.description}</p><div className="boutique-feature-price"><strong>❀ {hero.price_petals}</strong>{hero.pass_days&&<span>{hero.pass_days} days of Hanami+</span>}</div><div className="boutique-feature-actions">{buyButton(hero)}<button type="button" onClick={()=>setPreviewId(null)}>Reset preview</button></div></div></section>}<section className="boutique-shelf"><header><div><span className="eyebrow">{mode==='my-inventory'?'OWNED COLLECTION':'BROWSE'}</span><h2>{modeMeta[mode].title}</h2></div><strong>{visible.length} item{visible.length===1?'':'s'}</strong></header>{visible.length===0?<div className="rewards-empty">No items are available in this collection yet.</div>:<div className="boutique-orb-grid">{visible.map(item=>{const row=owned.get(item.id);return <article className={`boutique-orb-card rarity-${item.rarity} ${hero?.id===item.id?'previewing':''}`} key={item.id} onClick={()=>setPreviewId(item.id)}><div className="boutique-orb-art"><BoutiqueArtwork token={item.preview_token} name={item.name} type={item.item_type}/>{heart(item)}{item.is_new&&<span className="boutique-new-flag">NEW</span>}{row&&<span className="boutique-owned-flag">OWNED</span>}</div><div className="boutique-orb-copy"><span className="eyebrow">{item.collection_name||item.season}</span><h3>{item.name}</h3><p>{item.description}</p><div className="boutique-orb-meta"><span>{typeLabels[item.item_type]||item.item_type}</span><span>{item.rarity}</span>{item.pass_days&&<span>{item.pass_days} days</span>}</div></div><footer><strong>❀ {item.price_petals}</strong>{buyButton(item,true)}</footer></article>})}</div>}</section></>}</main>
}
